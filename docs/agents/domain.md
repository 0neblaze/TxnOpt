# Domain docs

This is a single-context repository. Before domain-oriented engineering work,
read a root `CONTEXT.md` if one exists and relevant records under `docs/adr/`.
Neither is currently required for the Stage 3.1 implementation.
