# Migration and provenance

This repository was separated from the school FURP repository so the advanced
Stage research can evolve and be released independently.

## Source identities

- School GitHub boundary:
  `7f4c72d7c391eb05317bc56209389fa889dbe902` (Week 5).
- Local D: checkpoint:
  `082d51f5d19e8848bc55067e896929431c4fee1c`.
- Latest migration source:
  `a5cf00f7580fc2632179495a739a110786ace87d`.
- Source tree:
  `e93a60f3c652f497626afd786ff23aa93e3005fb`.
- First advanced Stage commit:
  `ed23b8344a1ac9c750a7febeb9ff87a2c604215a`.

The public history contains 208 Stage commits and has no Week 1--5 parent
commits. Author identity and parentage were rewritten for publication, so
commit hashes changed. The CSV and JSON commit maps preserve the one-to-one
legacy-to-public relationship. Stable patch IDs match for all 207 replayed
non-root commits.

`source-file-disposition.csv` accounts for every one of the 878 tracked source
files. Its machine-readable summary records 808 byte-identical migrations, 24
publication-packaging modifications, four declared renames, 42
school-specific exclusions, and zero unclassified paths.

The rewritten root excludes school programme administration and Week 3--5
school reporting files. Scientific foundation code required by Stage 0 was
retained. Subsequent publication cleanup removed the remaining week-labelled
experiment runners after their shared benchmark helpers were moved to
`evrptw.benchmark`.

## Immutable historical evidence

Tracked historical manifests and baseline rows retain their original source
revisions and may retain non-secret historical machine paths. These bytes are
not rewritten to pretend that old experiments were generated by the public
history. Use the commit map and source inventory to reconcile those identities.

Large WSL staging and D: archive trees were not copied into Git and were not
modified during migration.
