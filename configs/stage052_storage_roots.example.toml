# Copy this file to stage052_storage_roots.local.toml and replace every
# REPLACE_* value with the identity observed in WSL2. The local file is ignored
# because it contains machine-specific absolute paths.

[roots.wsl_staging]
absolute_path = "/home/REPLACE_USER/stage052-active"
device_uuid = "REPLACE_WITH_WSL_EXT4_DEVICE_ID"
filesystem = "ext4"

[roots.d_archive]
absolute_path = "/mnt/d/FURP-2026-Yiyang-GUO-EVRP-TW-results"
device_uuid = "REPLACE_WITH_D_NVME_IDENTITY"
filesystem = "9p"
