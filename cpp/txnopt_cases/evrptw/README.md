# EVRPTW native case boundary

EVRPTW context packing, safe screening, and exact-charging kernels will live
here behind `txnopt-native-round-v1`. Generic transaction ownership must stay
in `cpp/txnopt_core`.

This structural directory does not rename or wrap the frozen Stage 5.2 ABI.
